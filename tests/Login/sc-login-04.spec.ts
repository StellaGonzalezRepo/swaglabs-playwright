import { test } from '@playwright/test';

/**
 * SC-LOGIN-04: checks the visual defects reproduced by visual_user.
 * Confluence reference: docs/login-de-usuario-casos-de-prueba/
 */
test.skip('SC-LOGIN-04 - visual user visual regression checks', async () => {
  // Requires a visual baseline and an agreed comparison strategy.
});